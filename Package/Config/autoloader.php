<?php

/**
 * 	有效位置：
 * 			/[PACKAGE_DIR]/Config/
 * 			/[APP_PATH]/Config/
 * 			/[APP_PATH]/[模块]/Config/
 *
 */

return array(

	//自动函数库加载
	'helpers'	=>	array(
		'/string'
	),

	//自动配置文件加载
	// config，db，uri，autoloader 已经不需要再次加载
	'config'	=>	array(

	),

);

